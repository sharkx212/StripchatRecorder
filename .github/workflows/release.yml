name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v0.2.0)'
        required: true

env:
  CARGO_TERM_COLOR: always

jobs:
  # ─────────────────────────────────────────────
  # Build main binary + all modules per platform
  # ─────────────────────────────────────────────
  build:
    name: Build (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-x86_64
            os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            artifact_suffix: linux-x86_64

          - name: linux-aarch64
            os: ubuntu-22.04
            target: aarch64-unknown-linux-gnu
            artifact_suffix: linux-aarch64
            cross: true

          - name: windows-x86_64
            os: windows-2022
            target: x86_64-pc-windows-msvc
            artifact_suffix: windows-x86_64

          - name: macos-x86_64
            os: macos-13
            target: x86_64-apple-darwin
            artifact_suffix: macos-x86_64

          - name: macos-aarch64
            os: macos-14
            target: aarch64-apple-darwin
            artifact_suffix: macos-aarch64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ── Node.js ──
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm

      - name: Install frontend dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      # ── Rust toolchain ──
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            src-tauri
            modules/contact_sheet
            modules/filter_short
            modules/notify_discord
            modules/notify_telegram
            modules/pp_utils
          key: ${{ matrix.target }}

      # ── Linux x86_64: system libraries ──
      - name: Install Linux dependencies (x86_64)
        if: matrix.os == 'ubuntu-22.04' && !matrix.cross
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libglib2.0-dev libgtk-3-dev libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev librsvg2-dev libpango1.0-dev \
            libcairo2-dev libgdk-pixbuf-xlib-2.0-dev libsoup-3.0-dev \
            pkg-config build-essential libssl-dev

      # ── Linux aarch64: cross-compile toolchain ──
      - name: Enable arm64 dpkg architecture
        if: matrix.cross
        run: sudo dpkg --add-architecture arm64

      - name: Fix apt sources for arm64 cross-compile
        if: matrix.cross
        run: |
          # Restrict existing amd64 sources to [arch=amd64] so apt doesn't try to
          # fetch arm64 indexes from security.ubuntu.com (which 404s for arm64).
          # Then add ports.ubuntu.com as the arm64 source.
          sudo sed -i 's/^deb /deb [arch=amd64] /' /etc/apt/sources.list
          echo "deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports jammy main restricted universe multiverse" | sudo tee -a /etc/apt/sources.list
          echo "deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports jammy-updates main restricted universe multiverse" | sudo tee -a /etc/apt/sources.list
          echo "deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports jammy-security main restricted universe multiverse" | sudo tee -a /etc/apt/sources.list

      - name: Install Linux cross-compile dependencies (aarch64)
        if: matrix.cross
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc-aarch64-linux-gnu g++-aarch64-linux-gnu \
            libglib2.0-dev:arm64 libgtk-3-dev:arm64 \
            libwebkit2gtk-4.1-dev:arm64 libayatana-appindicator3-dev:arm64 \
            librsvg2-dev:arm64 libpango1.0-dev:arm64 libcairo2-dev:arm64 \
            libgdk-pixbuf-xlib-2.0-dev:arm64 libsoup-3.0-dev:arm64 \
            libssl-dev:arm64 pkg-config
          echo "PKG_CONFIG_SYSROOT_DIR=/usr/aarch64-linux-gnu" >> $GITHUB_ENV
          echo "PKG_CONFIG_PATH=/usr/lib/aarch64-linux-gnu/pkgconfig" >> $GITHUB_ENV
          echo "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER=aarch64-linux-gnu-gcc" >> $GITHUB_ENV

      # ── Build main binary ──
      - name: Build Tauri binary
        run: npx tauri build --no-bundle --target ${{ matrix.target }}
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      # ── Build modules ──
      - name: Build modules (Unix)
        if: runner.os != 'Windows'
        run: |
          for module_dir in modules/contact_sheet modules/filter_short modules/notify_discord modules/notify_telegram; do
            if [ -f "$module_dir/Cargo.toml" ]; then
              echo "Building $module_dir..."
              cargo build --manifest-path "$module_dir/Cargo.toml" \
                --release --bins --target ${{ matrix.target }}
            fi
          done

      - name: Build modules (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          foreach ($mod in @("modules/contact_sheet","modules/filter_short","modules/notify_discord","modules/notify_telegram")) {
            if (Test-Path "$mod/Cargo.toml") {
              Write-Host "Building $mod..."
              cargo build --manifest-path "$mod/Cargo.toml" --release --bins --target ${{ matrix.target }}
            }
          }

      # ── Assemble package directory ──
      #
      # Layout:
      #   stripchat-recorder-{platform}/
      #   ├── stripchat-recorder(.exe)
      #   └── modules/
      #       ├── contact_sheet_v020(.exe)
      #       └── ...
      #
      - name: Assemble package (Unix)
        if: runner.os != 'Windows'
        run: |
          PKG="stripchat-recorder-${{ matrix.artifact_suffix }}"
          mkdir -p "$PKG/modules"

          # Main binary
          cp "src-tauri/target/${{ matrix.target }}/release/stripchat-recorder" "$PKG/"
          chmod +x "$PKG/stripchat-recorder"

          # Module binaries
          for module_dir in modules/contact_sheet modules/filter_short modules/notify_discord modules/notify_telegram; do
            find "$module_dir/target/${{ matrix.target }}/release" \
              -maxdepth 1 -type f -executable \
              ! -name '*.d' ! -name '*.rlib' ! -name '*.rmeta' \
              -exec cp {} "$PKG/modules/" \;
          done

          echo "=== Package contents ==="
          find "$PKG" -type f | sort

          # Archive
          tar -czf "${PKG}.tar.gz" "$PKG"

      - name: Assemble package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $pkg = "stripchat-recorder-${{ matrix.artifact_suffix }}"
          New-Item -ItemType Directory -Force -Path "$pkg/modules" | Out-Null

          # Main binary
          Copy-Item "src-tauri/target/${{ matrix.target }}/release/stripchat-recorder.exe" "$pkg/"

          # Module binaries
          foreach ($mod in @("modules/contact_sheet","modules/filter_short","modules/notify_discord","modules/notify_telegram")) {
            Get-ChildItem "$mod/target/${{ matrix.target }}/release/*.exe" -ErrorAction SilentlyContinue |
              Copy-Item -Destination "$pkg/modules/"
          }

          Write-Host "=== Package contents ==="
          Get-ChildItem -Recurse $pkg | Select-Object FullName

          # Archive
          Compress-Archive -Path $pkg -DestinationPath "${pkg}.zip"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: stripchat-recorder-${{ matrix.artifact_suffix }}
          path: stripchat-recorder-${{ matrix.artifact_suffix }}.*
          retention-days: 7

  # ─────────────────────────────────────────────
  # Create GitHub Release
  # ─────────────────────────────────────────────
  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/
          merge-multiple: true

      - name: List release assets
        run: ls -lh artifacts/

      - name: Determine tag
        id: tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "tag=${{ github.event.inputs.tag }}" >> $GITHUB_OUTPUT
          else
            echo "tag=${{ github.ref_name }}" >> $GITHUB_OUTPUT
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: Release ${{ steps.tag.outputs.tag }}
          draft: false
          prerelease: ${{ contains(steps.tag.outputs.tag, '-') }}
          generate_release_notes: true
          files: artifacts/*
